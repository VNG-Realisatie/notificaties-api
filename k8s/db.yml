---

# Deployment

kind: Deployment
apiVersion: extensions/v1beta1
metadata:
  name: postgres-notifications
  namespace: gemma
  labels:
    k8s-app: postgres-notifications
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: postgres-notifications
  template:
    metadata:
      name: postgres-notifications
      labels:
        k8s-app: postgres-notifications
    spec:
      volumes:
      - name: data
        hostPath:
          path: "/storage/gemma-postgres-notifications"
          type: ''
      containers:
      - name: postgres
        image: postgres:11
        env:
          - name: POSTGRES_PASSWORD
            value: <REDACTED>
        volumeMounts:
          - name: data
            mountPath: "/var/lib/postgresql/data"

---

# Service

kind: Service
apiVersion: v1
metadata:
  name: postgres-notifications
  namespace: gemma
  labels:
    k8s-app: postgres-notifications
spec:
  selector:
    k8s-app: postgres-notifications
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
